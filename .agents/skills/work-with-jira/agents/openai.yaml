interface:
  display_name: "Work With Jira"
  short_description: "Route and handle Jira Cloud issues safely"
  default_prompt: "Use $work-with-jira to route this Jira request to the correct site and handle it safely."
